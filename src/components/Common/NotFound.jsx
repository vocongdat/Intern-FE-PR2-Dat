import { Typography } from '@material-ui/core';

const NotFound = () => (
    <Typography variant='h1' component='div' sx={{ textAlign: 'center' }} gutterBottom>
        Not Found
    </Typography>
);

export default NotFound;
